// Domain types live in shared/types.ts so web, server, and the mobile app share one
// source of truth. This re-export keeps existing `./types` imports working unchanged.
export * from "../../shared/types";
